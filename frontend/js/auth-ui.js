// Authentication UI Logic for FirebaseUI
import { initAuth } from './auth.js';

// Initialize authentication
initAuth();

// The rest of the UI is handled by FirebaseUI in auth.js
console.log('✅ Auth UI initialized with FirebaseUI');
